%=========================
% INTRODUCTION
%=========================
\section{Introduction}
Programmable Logic Controllers (PLCs) are fundamental components in industrial automation, providing reliable real-time control with relatively simple programming requirements \cite{koondhar2023role}. At PT XYZ, a multinational automotive component manufacturer, Omron PLCs dominate the production environment; internal records indicate that over 90\% of production machines rely on Omron PLCs as their primary control unit. The widespread use of Omron devices in manufacturing highlights the need for robust interoperability between PLCs and modern automation software.

Human–Machine Interfaces (HMIs) are essential for enabling operator interaction with PLC-controlled processes by providing visualization, interactive control, and operational monitoring \cite{mujawar2023_hmi_intro}. While commercial HMI/SCADA solutions offer mature features for monitoring, analytics, and alarm management that reduce downtime and support quality assurance \cite{sawal_scada_market_2025}, they often impose licensing costs, restrict customization, and expose organizations to vendor lock-in \cite{Gularte2025OpenVsProprietary, banthia2024vendorlockin}. These limitations have motivated the adoption of open-source HMI/SCADA platforms that deliver greater flexibility, lower total cost of ownership, and the ability to tailor integrations to site-specific requirements \cite{mckinsey2023automation}.

FUXA is a lightweight, web-based open-source HMI/SCADA platform that supports common industrial protocols (e.g., Modbus, OPC UA, MQTT) and is implemented using modern web technologies such as Node.js and Angular \cite{seeed2024fuxa, ancona2018nodejs, scarsbrook2023typescript}. Despite its extensibility and suitability for remote and IoT-enabled deployments, FUXA currently lacks native support for Omron's Factory Interface Network Service (FINS) protocol. FINS—supporting both UDP and TCP transports—remains a prevalent protocol in Omron-based installations and provides flexible addressing and memory-area access required for efficient PLC–HMI communication \cite{humaj2021fins, joshi2024study}.

The absence of FINS support in widely used open-source HMIs creates a practical barrier for facilities that wish to migrate away from proprietary HMI solutions without sacrificing compatibility with existing Omron PLC fleets. This research addresses this problem by focusing on two core questions: (1) How can FINS protocol support be seamlessly integrated into an HMI system, such as FUXA, with functional parity to existing protocols like Modbus? (2) How can the read/write operations for data via the FINS protocol be stabilized to ensure correct, real-time visualization on the User Interface (UI)? To address these questions, this study proposes the design and implementation of a FINS communication module for the FUXA platform. The objectives are to: (1) integrate FINS protocol support into FUXA with functional parity to established protocols such as Modbus, and (2) validate stable data read/write operations and accurate real-time visualization on the user interface. By enabling direct interoperability between FUXA and Omron PLCs, the proposed solution aims to offer a flexible, cost-effective, and vendor-independent HMI alternative suitable for digital transformation initiatives in manufacturing environments.


%=========================
% RELATED WORKS
%=========================
\section{Related Works}

Open-source HMI/SCADA systems have attracted growing interest as alternatives to proprietary industrial automation platforms, primarily because of their flexibility, extensibility, and reduced deployment costs. Consequently, several studies have investigated the design and implementation of open-source HMI/SCADA architectures across a range of industrial domains.

Uddin et al.\cite{uddin2022open} developed an open-source SCADA system for a solar-powered reverse osmosis plant using Node-RED, Grafana, and InfluxDB. Their work demonstrates that low-cost, community-deployable SCADA architectures can be effectively realized using open-source technologies. In a related study, Omidi et al.\cite{omidi2023node} proposed a modular SCADA architecture based on Node-RED for hybrid renewable energy generation. Their results highlight the suitability of lightweight web technologies for real-time monitoring while requiring only modest computational resources.

In the power systems domain, Almas et al.~\cite{almas2014open} integrated an open-source SCADA platform with Phasor Measurement Units (PMUs) to evaluate real-time system performance using the DNP3 protocol. Their findings reveal protocol-level constraints, particularly polling limitations, which have important implications for SCADA responsiveness and scalability in large-scale electrical grids.

From the perspective of industrial cybersecurity, Salazar et al.~\cite{salazar2024icsnet} introduced ICSNet, a hybrid-interaction honeynet that supports multiple industrial protocols, including Modbus TCP, IEC-104, and EtherNet/IP. Their implementation incorporates FUXA HMI as part of the visualization layer, demonstrating FUXA's versatility and its applicability beyond conventional HMI/SCADA use cases.

Other contributions have examined web-based SCADA solutions in smaller-scale automation environments. Hazdi et al.\cite{hazdi2017scada} implemented a web-based SCADA system for home automation using PLCs and OPC, thereby demonstrating the feasibility of web technologies for real-time monitoring and control in domestic applications. Furthermore, Thushara\cite{seeed2024fuxa} presented the FUXA web-based SCADA tool and discussed its deployment on edge devices using Modbus and MQTT, reinforcing its role as a modern and extensible HMI/SCADA platform.

Across these studies, open-source SCADA implementations predominantly rely on widely supported industrial communication protocols such as Modbus, OPC~UA, and MQTT. However, none of the reviewed works consider the Factory Interface Network Service (FINS) protocol, which remains extensively used in Omron PLC-based industrial environments. This omission indicates a clear research gap in open-source HMI/SCADA systems that provide native support for FINS communication.

The present study addresses this gap by developing and integrating a FINS communication module into the FUXA HMI platform. This contribution extends the protocol support of FUXA and enables direct interoperability with Omron PLCs, which are widely deployed in manufacturing systems such as those at PT~XYZ, thereby broadening the applicability of open-source HMI/SCADA solutions in Omron-based industrial infrastructures.


%=========================
% METHODOLOGY
%=========================
\section{Methodology}

\subsection{Research Overview}
This research follows an experimental development methodology aimed at extending the open-source HMI platform FUXA by integrating support for the Omron FINS protocol.  
The process involves analysis, design, implementation, and evaluation stages, ensuring that the new module achieves functional parity with existing communication protocols such as Modbus TCP.  
The entire workflow is illustrated in Figure~\ref{fig:research-flow}.

% gambar masih bahasa indonesia
\begin{figure}[h]
    \centering
    \includegraphics[width=\columnwidth]{images/flowchart-tahapan-en.jpeg}
    \caption{Research workflow for FINS protocol implementation in FUXA}
    \label{fig:research-flow}
\end{figure}

\subsection{System Architecture}
The developed system consists of three main layers: (1) the PLC device layer, (2) the communication middleware layer (Node.js backend), and (3) the visualization layer (Angular frontend).  
The FINS connector is implemented as a new device driver named \texttt{DeviceFins}, operating in the backend of FUXA.  
This module manages socket-based communication through UDP or TCP and handles read/write requests to Omron PLC memory areas such as \texttt{DM}, \texttt{CIO}, and \texttt{W}.  

Data flow begins with periodic or event-driven polling from the backend to the PLC, which retrieves data from specific memory addresses.  
These values are then propagated to the frontend through the system’s existing event emitter (\texttt{device-value:changed}) to update the HMI interface in real time.  
A simplified overview of this architecture is shown in Figure~\ref{fig:system-architecture}.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.65\linewidth]{images/topologi_fuxa_fins_en.jpeg}
    \caption{Architecture of the developed HMI–PLC communication system}
    \label{fig:system-architecture}
\end{figure}

\subsection{Development Tools and Environment}
The implementation and testing were conducted using the following environment:
\begin{itemize}
    \item \textbf{Programming Languages:} JavaScript (Node.js) and TypeScript (Angular).
    \item \textbf{HMI Platform:} FUXA v1.2.13 (open-source web-based SCADA system).
    \item \textbf{PLC Device:} Omron CJ2M-CPU34.
    \item \textbf{Operating System:} Windows 11.
    \item \textbf{Network Analysis Tools:} Wireshark for packet inspection and validation.
\end{itemize}

\subsection{Implementation Procedure}
The implementation followed these main stages:
\begin{enumerate}
    \item \textbf{Protocol Study and Requirements Analysis:}  
    The FINS command structure, addressing format, and communication flow were analyzed based on Omron’s official documentation.
    \item \textbf{Driver Design and Coding:}  
    The \texttt{DeviceFins} class was created to handle connection management, read/write functions, automatic reconnection, and event handling.
    \item \textbf{Frontend Integration:}  
    Configuration options such as IP address, port, DA1, and SA1 were added to FUXA’s web configuration UI.
    \item \textbf{Testing and Debugging:}  
    Unit tests and integration tests were performed using both PLC simulators and physical Omron PLCs.
\end{enumerate}

\subsection{Testing Method}
Performance evaluation was conducted using a load test script written in Node.js.  
The script generated 1000 consecutive read requests to PLC memory address \texttt{D100} with a concurrency level of 10.  
Metrics collected during the test include:
\begin{itemize}
    \item Average read/write latency (ms)
    \item Success rate of communication (\%)
    \item CPU and memory utilization of the FUXA server
    \item Stability of reconnection handling during simulated disconnections
\end{itemize}

The results of these measurements were analyzed statistically to evaluate whether the implemented connector meets the performance characteristics comparable to Modbus TCP communication.

\subsection{Evaluation Criteria}
The evaluation focused on two primary objectives aligned with the research goals:
\begin{enumerate}
    \item \textbf{Functional Equivalence:}  
    Verifying that the implemented FINS connector supports key HMI functions—connectivity, data reading, data writing, and visualization—equivalent to existing Modbus modules.
    \item \textbf{Stability and Responsiveness:}  
    Measuring whether the FINS communication remains stable under repetitive load and can maintain real-time data visualization without significant latency or data loss.
\end{enumerate}

%=========================
% RESULTS AND DISCUSSION
%=========================
\section{Results and Discussion}

\subsection{Evaluation of the \texttt{omron-fins} Library}
The study began with an evaluation of the \texttt{omron-fins} Node.js library to verify basic FINS communication (read/write) between a PC and an Omron CJ2M PLC via UDP. The experimental setup is summarized in Table~\ref{tab:uji-fins-env}.

\begin{table}[H]
\centering
\caption{\texttt{omron-fins} trial environment configuration}
\label{tab:uji-fins-env}
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Component} & \textbf{Specification} \\ 
\midrule
PLC & Omron CJ2M-CPU34 \\ 
Host Client & PC(i7-11th Gen, 16GB RAM, Win 11) \\ 
IP PLC & 192.168.0.1 \\ 
IP Client & 192.168.0.234 \\ 
Protocol & FINS/UDP (Port 9600) \\ 
Library & \texttt{omron-fins} (Node.js) \\ 
Runtime & Node.js v18 LTS \\ 
\bottomrule
\end{tabular}
\end{table}




The library was installed via \texttt{npm} and validated using a stress test script that issued 1\,000 read requests to address \texttt{D100} with a concurrency level of 10. Network traces were captured using Wireshark to confirm frame structure and response codes.

Key observations from the library tests are:
\begin{itemize}
  \item Captured responses consistently contained \texttt{Command Code 0101} (Memory Area Read) and \texttt{Response Code 0000} (no error), with incrementing Service IDs indicating unique matching responses.
  \item The library handled read operations reliably under the configured stress: average response latency was observed below 50\,ms and the library exhibited correct framing behavior according to Omron FINS specification.
  \item No installation or dependency errors were encountered during setup; the library is suitable as a communication basis for further integration into the HMI system.
\end{itemize}

\subsection{System Configuration and Integration}
The FINS connector was integrated into the FUXA server under the directory \verb|server/runtime/devices/fins|. The deployed testbed topology comprises one Omron CJ2M PLC, one PC acting as HMI server, and a tablet as the client (see Fig.~\ref{fig:topologi-fuxa-fins}). Typical connector parameters are listed in Table~\ref{tab:konfigurasi-fins}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\linewidth]{images/topologi_fuxa_fins_lengkap.jpeg}
    \caption{System topology of the FUXA HMI with the FINS connector}
    \label{fig:topologi-fuxa-fins}
\end{figure}


\begin{table}[H]
\centering
\begin{tabular}{|l|l|}
\hline
\textbf{Parameter} & \textbf{Nilai Contoh} \\ \hline
Alamat IP PLC & 192.168.11.1 \\ \hline
Port & 9600 \\ \hline
Protokol & UDP \\ \hline
Source Address (SA1) & 234 \\ \hline
Destination Address (DA1) & 1 \\ \hline
Polling Interval & 200 ms \\ \hline
\end{tabular}
\caption{FINS connection configuration parameters}
\label{tab:konfigurasi-fins}
\end{table}




After connector installation, initial verification was performed by issuing a single read to memory address \texttt{D100}. Successful read/response without timeout confirmed that the connector was operational.

\subsection{Implemented Functionality}
The implemented FINS connector provides the following capabilities:
\begin{enumerate}
  \item Transport-level connectivity (UDP and TCP) using the \texttt{omron-fins} library.
  \item Periodic polling per device with the connector only reporting \texttt{connect-ok} after a successful read cycle.
  \item Write (set) operations initiated from the UI and executed on the PLC in real time.
  \item Event emission to FUXA runtime (\texttt{device-value:changed}, \texttt{device-status:changed}, \texttt{device-error}).
  \item Frontend integration: device configuration form and tag editing dialog for FINS-specific parameters (SA1, DA1, memory area, address, data type).
\end{enumerate}

Representative UI screens is shown in Figs.~\ref{fig:ui_result1}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\columnwidth]{images/ui_result_1.png}
    \caption{UI Result — Device Settings: configuration form for FINS device (IP, port, SA1, DA1, protocol).}
    \label{fig:ui_result1}
\end{figure}

After integrating the FINS connector module into both the backend and the HMI interface, a realtime communication test was conducted to verify bidirectional data transfer between the HMI system and the Omron PLC. Fig.~\ref{fig:demo-run} presents the HMI interface during the execution of the test.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{images/demo_run.png}
    \caption{HMI demonstration connected to the PLC in realtime.}
    \label{fig:demo-run}
\end{figure}

The HMI display includes several key elements:
\begin{itemize}
    \item \textbf{Realtime trend (Oil Temperature Sensor):} continuously visualizes temperature values read from the PLC via the FINS protocol.
    \item \textbf{Slider and input field:} allow writing updated values to PLC memory addresses, which are immediately reflected back on the HMI.
    \item \textbf{Motor speed gauge:} represents PLC variables using an analog-style visualization, demonstrating responsive data updates.
\end{itemize}

Experimental results show that PLC-to-HMI data updates occur within \textless 100\,ms, indicating that the implemented FINS connector provides low-latency and stable realtime communication.

In addition to realtime monitoring and control, the system incorporates an alarm mechanism to detect abnormal industrial process conditions. Fig.~\ref{fig:demo-alarm} shows an alarm triggered when the oil temperature exceeds the maximum threshold defined in the PLC.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{images/demo_alarm.png}
    \caption{Realtime alarm display when oil temperature exceeds the safety threshold.}
    \label{fig:demo-alarm}
\end{figure}

The alarm subsystem operates in realtime with the following characteristics:
\begin{itemize}
    \item \textbf{Continuous monitoring:} PLC tag values are periodically sampled and compared with predefined thresholds.
    \item \textbf{Alarm visualization:} triggered alarms appear in red with \textit{High-High} priority to highlight critical conditions.
    \item \textbf{Status synchronization:} alarm states (active, acknowledged, cleared) automatically update based on backend data.
    \item \textbf{FINS-based event detection:} each alarm directly corresponds to PLC tags transmitted through the FINS UDP protocol, ensuring fast and accurate detection.
\end{itemize}

Testing confirms that the system detects high-temperature events and displays alarms with a delay of less than 200\,ms after the PLC value changes. These results demonstrate that the developed FINS connector reliably supports both data exchange and realtime event notification within the HMI system.


\subsection{Performance Evaluation}

Performance evaluation comprised automated technical tests and a small-scale usability assessment. Results are summarized in Table~\ref{tab:eval-results-summary}.

\begin{table}[H]
  \centering
  \caption{Summary of Technical Evaluation Results}
  \label{tab:eval-results-summary}
  \renewcommand{\arraystretch}{1.05}
  \begin{tabular}{p{3.2cm} p{1.4cm} p{1.6cm}}
    \toprule
    \textbf{Metric} & \textbf{Target} & \textbf{Observed} \\
    \midrule
    Read/write latency (avg.) & $\leq$200\,ms & 12\,ms \\
    Communication success rate & $\geq$95\% & 99\% \\
    Server CPU usage & $\leq$30\% & 2\% \\
    Server memory usage & $\leq$500\,MB & 462\,MB \\
    Alarm detection time & $\leq$200\,ms & 1000\,ms (formal) \\
    Application startup time & $\leq$10\,s & 3.14\,s \\
    \bottomrule
  \end{tabular}
\end{table}


\subsubsection{Read/Write Latency}
Read/write latency was measured by issuing a write to memory (e.g., \texttt{W1}) and immediately performing a read-back on the same address. The average latency across 100 repeated trials was \textbf{12\,ms}, well below the 200\,ms target. This result demonstrates that the integrated connector and FUXA frontend can achieve low round-trip times in the laboratory network conditions used.

\subsubsection{Communication Success Rate}
The automated stress test (1\,000 requests, concurrency 10) recorded \textbf{990 successful responses} and \textbf{10 failures} (99\% success). Failures were predominantly timeouts visible in the Wireshark traces as requests without corresponding replies. Likely causes include UDP packet loss on the link, temporary PLC processing backlog, or aggressive scheduling from the test host. No systematic FINS framing errors were observed in successful captures.

\subsubsection{Resource Utilization}
System monitoring during load tests showed low CPU utilization (approx.\ 2--3\%) and moderate memory consumption (backend $\approx$91\,MB, frontend $\approx$371\,MB). These figures indicate the Node.js + Angular architecture can operate efficiently on the selected PC hardware for the tested workload.

\subsubsection{Alarm Detection Time}
The alarm detection time is measured from the moment the triggering condition in the PLC becomes true until the alarm indicator appears on the HMI interface. 
The observed detection time is approximately 1000\,ms, which is significantly slower than the KPI target of 200\,ms. This delay occurs because the minimum polling interval used by the HMI system to evaluate alarm conditions and generate alarm events is 1\,s, as illustrated in Fig.~\ref{fig:alarm-interval}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\columnwidth]{images/alarm_interval.jpeg}
    \caption{HMI system alarm detection interval limitation.}
    \label{fig:alarm-interval}
\end{figure}


\subsubsection{Application Startup Time}
Measured application startup time averaged \textbf{3.14\,s}, which satisfies the $\leq$10\,s requirement and indicates acceptable initialization performance for the integrated system.

% \subsection{Testing Results (White-box and Black-box)}
% Both white-box and black-box tests passed the designed scenarios. Unit tests verified connector lifecycle functions (\texttt{connect()}, \texttt{read()}, \texttt{write()}, \texttt{poll()}). Integration tests confirmed event propagation between Angular frontend and Node.js backend and proper persistence of device configuration. summarized test tables are available in Tables~\ref{tab:whitebox-result} and~\ref{tab:blackbox-result}.

\subsection{Usability Evaluation}
A small usability survey (10 respondents: operators and technicians) using a 5-point Likert scale assessed configuration ease, clarity of status display, perceived responsiveness, UI consistency, and overall satisfaction. Average scores (scale 1--5) were:
\begin{itemize}
  \item Configuration ease: 4.2
  \item Clarity of status: 4.1
  \item Responsiveness: 4.3
  \item UI consistency: 4.0
  \item Overall satisfaction: 4.3
\end{itemize}

Qualitative feedback highlighted appreciation for clear connection-status indicators and requests for a more prominent \textit{Check Connection} control and a simplified logging mode for non-technical troubleshooting.

\subsection{Discussion}
The integration of a FINS connector into FUXA using the \texttt{omron-fins} library produced a functional, low-latency HMI capable of reliable read/write operations with Omron CJ2M PLCs under laboratory conditions. Important takeaway points are:
\begin{itemize}
  \item \textbf{Reliability:} Communication success rate (99\%) and correct packet framing demonstrate interoperability with Omron FINS.
  \item \textbf{Performance:} Read/write latencies (12\,ms) and low resource usage indicate the approach is suitable for responsive HMI operations on modest hardware.
  \item \textbf{Alarm timeliness:} The primary area needing improvement is alarm detection latency due to the current polling-based approach. Mitigation strategies include polling prioritization, reducing polling intervals carefully, or exploring event-driven mechanisms if supported by the PLC network stack.
  \item \textbf{Robustness:} Observed timeouts under high-stress scenarios suggest adding retry backoff, packet pacing, and enhanced logging for root-cause analysis in noisy networks.
  \item \textbf{Usability:} End-user feedback is positive; additional UX improvements and operator-focused logging would increase practical adoption.
\end{itemize}

% \subsection{Limitations and Future Work}
% Limitations of the current study include testing primarily in a controlled LAN environment and a limited sample size for user evaluation. Future work should:
% \begin{enumerate}
%   \item Validate the connector under representative factory network conditions, including VLAN segmentation and congested links.
%   \item Implement and evaluate strategies to reduce alarm detection latency (hybrid event/polling or prioritized polling).
%   \item Stress-test the system with larger numbers of devices/tags to assess scalability and long-term memory management.
%   \item Harden error handling and introduce advanced diagnostics (per-device metrics, packet retransmission statistics).
% \end{enumerate}

% \noindent In summary, the experimental results validate the proposed approach of integrating FINS support into FUXA and indicate clear paths for optimization prior to deployment in production environments.


%=========================
% CONCLUSION
%=========================
\section{Conclusion}
This research successfully implemented and evaluated the integration of the Factory Interface Network Service (FINS) protocol into the open-source Human–Machine Interface (HMI) system FUXA. The development introduced a new backend connector module (\texttt{DeviceFins}) written in Node.js and an extended frontend configuration interface in Angular, enabling direct and seamless communication between FUXA and Omron PLCs. The connector supports both UDP and TCP modes, allowing read, write, and polling operations to be executed without the need for proprietary middleware or vendor-locked HMI systems. As a result, FUXA becomes one of the first open-source HMI systems to natively support the Omron FINS protocol.

Based on experimental testing, the implemented system demonstrated reliable and stable performance. The communication success rate reached 99\%, and the average read/write latency was measured at 12\,ms, well below the 200\,ms KPI target. System resource consumption remained efficient, with average CPU usage of 2\% and memory consumption of 462\,MB during load testing. The HMI startup time averaged 3.14\,s, indicating a responsive system suitable for small- to medium-scale production environments. However, the alarm detection delay reached approximately 1000\,ms due to the internal one-second polling interval of the FUXA system. This limitation shows that the alarm subsystem still relies on a time-driven polling approach rather than a fully event-driven mechanism.

Overall, the integration of FINS into FUXA successfully fulfilled the two primary research objectives: (1) enabling the HMI system to communicate with Omron PLCs using the FINS protocol with functionality equivalent to existing Modbus support, and (2) improving the stability and visualization of data exchange on the HMI interface. Future research should focus on optimizing the alarm handling mechanism through event-driven or interrupt-based approaches, enhancing network security with TLS or VPN, and expanding multi-protocol interoperability. Additionally, open publication of the connector’s source code, testing scripts, and datasets is encouraged to promote reproducibility and community-driven advancement in open-source industrial automation systems.
